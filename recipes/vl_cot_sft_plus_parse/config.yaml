# LLM parser config
parse_model: qwen25_32b_instruct
parse_base_url: http://100.102.249.23:21003/v1/
parse_api_key: dummy
parse_temperature: 0.0
parse_max_tokens: 8096
parse_max_workers: 256

# Sampling config
model: qwen3_vl_235b_a22b_thinking
base_url: http://100.102.249.23:21001/v1
api_key: dummy
n_samples: 2
temperature: 0.6
max_tokens: 10240
semaphore_per_sampler: 64

# Data path config
# abs_image_path_field: absolute image path field (supports nesting, e.g. meta_info.abs_image_path)
# max_image_size_mb: max image size in MB; larger images are marked failed (default 20MB)
abs_image_path_field: abs_path
max_image_size_mb: 20.0

# LLM judge config
judge_model: qwen25_32b_instruct
judge_base_url: http://100.102.249.23:21003/v1/
judge_api_key: dummy
judge_temperature: 0.0
judge_max_tokens: 10
verifier_max_workers: 256  # VerifierStage thread pool size (per-item in batch)

# Formatting config
pass_threshold: 1.0

# Retry config
max_retries: 2

# Pipeline config
batch_size: 128  # Items per batch
concurrency: 2  # Default concurrency (stage actor count)
sampler_concurrency: 10  # SamplerStage concurrency
verifier_concurrency: 2  # VerifierStage concurrency
